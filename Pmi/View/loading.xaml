<UserControl x:Class="Pmi.View.loading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pmi.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Name="LoadingView" Grid.Row="1">
        <TextBlock Text="{Binding Status}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="30,30,30,80"/>
        <ProgressBar Value="{Binding Progress}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="30" Height="30" Minimum="0" Maximum="16"/>
    </Grid>
</UserControl>
